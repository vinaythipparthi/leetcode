class Solution {
    public boolean uniformArray(int[] nums1) {
        //if even a single element in nums1 is odd then nums2 can have all odd elements
        //because odd - even = odd & odd - odd = odd.
        //if not then all even
        //both are true
        return true;
    }
}